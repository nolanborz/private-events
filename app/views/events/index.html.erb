# app/views/events/index.html.erb
<h1>Upcoming Events (Mandatory Attendance)</h1>

<% @events.each do |event| %>
  <div>
    <h2><%= event.title %></h2>
    <p>Date: <%= event.date %></p>
  </div>
<% end %>

<%= link_to "Create New Event", new_event_path %>
